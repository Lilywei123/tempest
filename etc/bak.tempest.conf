[DEFAULT]
debug = True
verbose=false
#log_file = tempest.log
use_stderr = False
lock_path = /opt/stack/data/tempest


[boto]
ssh_user = cirros
instance_type = m1.nano
aki_manifest = cirros-0.3.2-x86_64-vmlinuz.manifest.xml
ami_manifest = cirros-0.3.2-x86_64-blank.img.manifest.xml
ari_manifest = cirros-0.3.2-x86_64-initrd.manifest.xml
s3_materials_path = /home/lily/devstack/files/images/s3-materials/cirros-0.3.2
s3_url = http://192.168.1.100:3333
ec2_url = http://192.168.1.100:8773/services/Cloud
http_socket_timeout = 30
build_timeout = 196




[cli]
cli_dir = /usr/local/bin



[compute]
ssh_connect_method = fixed
flavor_ref_alt = 84
flavor_ref = 42
image_alt_ssh_user = cirros
image_ref_alt = e73a4634-2d09-4a38-9d20-667d9e78463f
image_ssh_user = cirros
image_ref = e73a4634-2d09-4a38-9d20-667d9e78463f
ssh_timeout = 196
ip_version_for_ssh = 4
network_for_ssh = private
ssh_user = cirros
allow_tenant_isolation = True
build_timeout = 196




[compute-admin]
tenant_name = admin
password = esit
username = admin




[compute-feature-enabled]
block_migration_for_live_migration = False
change_password = False
live_migration = False
resize = True
api_v3 = False




[dashboard]
login_url = http://192.168.1.100/auth/login/
dashboard_url = http://192.168.1.100/






[identity]
auth_version = v2
admin_domain_name = Default
admin_tenant_id = 6100c723a0234c43833d4be22f43de7f
admin_tenant_name = admin
admin_password = esit
admin_username = admin
alt_tenant_name = alt_demo
alt_password = esit
alt_username = alt_demo
tenant_name = demo
password = esit
username = demo
uri_v3 = http://192.168.1.100:5000/v3/
uri = http://192.168.1.100:5000/v2.0/

#
# Options defined in tempest.config
#

# Catalog type of the Identity service. (string value)
#catalog_type=identity

# Set to True if using self-signed SSL certificates. (boolean
# value)
#disable_ssl_certificate_validation=false

# Full URI of the OpenStack Identity API (Keystone), v2
# (string value)
#uri=<None>

# Full URI of the OpenStack Identity API (Keystone), v3
# (string value)
#uri_v3=<None>

# Identity API version to be used for authentication for API
# tests. (string value)
#auth_version=v2

# The identity region name to use. Also used as the other
# services' region name unless they are set explicitly. If no
# such region is found in the service catalog, the first found
# one is used. (string value)
#region=RegionOne

# The endpoint type to use for the identity service. (string
# value)
#endpoint_type=publicURL

# Username to use for Nova API requests. (string value)
#username=<None>

# Tenant name to use for Nova API requests. (string value)
#tenant_name=<None>

# Role required to administrate keystone. (string value)
#admin_role=admin

# API key to use when authenticating. (string value)
#password=<None>

# Domain name for authentication (Keystone V3).The same domain
# applies to user and project (string value)
#domain_name=<None>

# Username of alternate user to use for Nova API requests.
# (string value)
#alt_username=<None>

# Alternate user's Tenant name to use for Nova API requests.
# (string value)
#alt_tenant_name=<None>

# API key to use when authenticating as alternate user.
# (string value)
#alt_password=<None>

# Alternate domain name for authentication (Keystone V3).The
# same domain applies to user and project (string value)
#alt_domain_name=<None>

# Administrative Username to use for Keystone API requests.
# (string value)
#admin_username=<None>

# Administrative Tenant name to use for Keystone API requests.
# (string value)
#admin_tenant_name=<None>

# API key to use when authenticating as admin. (string value)
#admin_password=<None>

# Admin domain name for authentication (Keystone V3).The same
# domain applies to user and project (string value)
#admin_domain_name=<None>


[identity-feature-enabled]

#
# Options defined in tempest.config
#

# Does the identity service have delegation and impersonation
# enabled (boolean value)
#trust=true

# Is the v2 identity API enabled (boolean value)
#api_v2=true

# Is the v3 identity API enabled (boolean value)
#api_v3=true





[network]
default_network = 10.0.0.0/24
public_router_id = 
public_network_id = 
tenant_networks_reachable = false
api_version = 2.0




[network-feature-enabled]
api_extensions = all
ipv6_subnet_attributes = True
ipv6 = True




[orchestration]
build_timeout = 900
instance_type = m1.heat
image_ref = Fedora-x86_64-20-20140618-sda




[scenario]
large_ops_number = 0
aki_img_file = cirros-0.3.2-x86_64-vmlinuz
ari_img_file = cirros-0.3.2-x86_64-initrd
ami_img_file = cirros-0.3.2-x86_64-blank.img
img_dir = /home/lily/devstack/files/images/cirros-0.3.2-x86_64-uec




[service_available]
neutron = False
heat = True
ceilometer = False
swift = False
cinder = True
nova = True
glance = True
horizon = True




[telemetry]
too_slow_to_test = False



[volume]
build_timeout = 196




[volume-feature-enabled]
backup = False




